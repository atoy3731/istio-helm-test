defaultNamespace: cert-manager

labels:
  app: public-cert-manager-clusterissuer

kustomize:
  dir: ./resources

dependsOn:
  - selector:
      matchLabels:
        app: public-cert-manager-cert-manager