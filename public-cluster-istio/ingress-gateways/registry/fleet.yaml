defaultNamespace: istio-system

labels:
  app: public-istio-igw-registry

helm:
  repo: https://istio-release.storage.googleapis.com/charts
  chart: gateway
  version: 1.23.2
  valuesFiles:
  - values.yaml

dependsOn:
  - selector:
      matchLabels:
        app: public-istio-istiod
